name: Workflow-Schedule

on:
  schedule:
    - cron: "*/5 * * * *"   # cada 5 minutos

jobs:
  ejecutar:
    runs-on: ubuntu-22.04
    steps:
      - name: Mensaje
        run: echo "Ejecución programada"
